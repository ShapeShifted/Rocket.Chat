version: "3.8"
services:
  rocketchat-dev:
    build:
      context: .
      dockerfile: Dockerfile.app
    image: rocketchat-dev:local
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    restart: unless-stopped